[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "psygrid-windowau"
version = "0.1.0"
description = "PSYGRID WindowAU technical XAUUSD intraday intelligence engine"
requires-python = ">=3.12"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.setuptools.packages.find]
include = ["psygrid_windowau*"]
